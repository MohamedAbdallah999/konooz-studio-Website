import {useState} from 'react';import {useLiveQuery} from 'dexie-react-hooks';import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';import {TrendingUp,ReceiptText} from 'lucide-react';import {db,refundSale} from '../db';import type {Sale} from '../types';import {Receipt} from './Receipt';
export function Sales(){const [selected,setSelected]=useState<Sale|null>(null);const sales=useLiveQuery(()=>db.sales.filter(s=>!s.deletedAt).reverse().sortBy('createdAt'))??[];const groups=new Map<string,number>();for(const s of sales){const d=new Date(s.createdAt).toLocaleDateString('en',{month:'short',day:'numeric'});groups.set(d,(groups.get(d)??0)+s.totalAmount)}const chart=[...groups].slice(-14).map(([date,total])=>({date,total})),total=sales.reduce((x,s)=>x+s.totalAmount,0);
const showReceipt=async(sale:Sale)=>{const lines=sale.items?.length?sale.items:await db.saleItems.where('saleId').equals(sale.id).toArray();const detailed=await Promise.all(lines.map(async line=>{const variant=await db.variants.get(line.itemVariantId),item=variant?await db.items.get(variant.itemId):undefined;return {...line,itemVariant:variant?{...variant,item}:undefined}}));setSelected({...sale,items:detailed})};
const refund=async()=>{if(!selected||!confirm(`Refund receipt #${selected.id.slice(0,8).toUpperCase()}? The sold items will return to stock and this sale will be removed from revenue.`))return;await refundSale(selected);setSelected(null)};
if(selected)return <Receipt sale={selected} onClose={()=>setSelected(null)} onRefund={refund}/>;
return <><section className="section-head"><div><p className="eyebrow">THE LEDGER</p><h2>Sales & reporting</h2><p>Select any transaction to view, print, download, or refund its receipt.</p></div></section><div className="report-stats"><article><TrendingUp/><span>Total revenue</span><strong>{total.toLocaleString()} EGP</strong></article><article><ReceiptText/><span>Completed sales</span><strong>{sales.length}</strong></article></div><section className="chart-card"><header><div><p className="eyebrow">REVENUE RHYTHM</p><h3>Recent daily sales</h3></div></header><div className="chart"><ResponsiveContainer><BarChart data={chart}><XAxis dataKey="date" axisLine={false} tickLine={false}/><YAxis axisLine={false} tickLine={false}/><Tooltip contentStyle={{background:'#171511',border:0,color:'#fff'}}/><Bar dataKey="total" fill="#B8963E" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="ledger"><header><p className="eyebrow">RECENT RECEIPTS</p><h3>Transaction history</h3></header>{sales.map(s=><article key={s.id} role="button" tabIndex={0} onClick={()=>showReceipt(s)} onKeyDown={e=>(e.key==='Enter'||e.key===' ')&&showReceipt(s)}><div className="receipt-icon"><ReceiptText/></div><div><b>#{s.id.slice(0,8).toUpperCase()}</b><span>{new Date(s.createdAt).toLocaleString()}</span></div><span>{s.items.length} lines</span><strong>{s.totalAmount.toLocaleString()} EGP</strong><i className={s.syncStatus}>{s.syncStatus}</i></article>)}</section></>}
