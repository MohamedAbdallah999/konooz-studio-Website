import {useLiveQuery} from 'dexie-react-hooks';import {db} from '../db';import {SilkScene} from '../components/SilkScene';import {ArrowUpRight,Package,ShoppingBag,TriangleAlert} from 'lucide-react';import {Link} from 'react-router-dom';import {paymentEvents} from '../payments';
const money=(n:number)=>new Intl.NumberFormat('en-EG',{style:'currency',currency:'EGP',maximumFractionDigits:0}).format(n);
export function Dashboard(){const data=useLiveQuery(async()=>{const items=await db.items.filter(x=>!x.deletedAt).toArray(),activeItemIds=new Set(items.map(item=>item.id)),variants=await db.variants.filter(x=>!x.deletedAt&&activeItemIds.has(x.itemId)).toArray(),sales=await db.sales.filter(x=>!x.deletedAt).toArray();const today=new Date().toDateString(),todaySales=sales.filter(s=>new Date(s.createdAt).toDateString()===today),revenue=sales.flatMap(paymentEvents).filter(event=>new Date(event.date).toDateString()===today).reduce((sum,event)=>sum+event.amount,0);return{models:items.length,pieces:variants.reduce((s,v)=>s+v.stockQuantity,0),low:variants.filter(v=>v.stockQuantity<=3).length,revenue,sales:todaySales.length}})??{models:0,pieces:0,low:0,revenue:0,sales:0};return <><section className="hero"><SilkScene/><div className="hero-copy"><p className="eyebrow">THE STYLE YOU LOVE</p><h2>Every dress has<br/>a <em>story.</em></h2><p>Keep the collection moving-from atelier floor to a customer's wardrobe.</p><Link className="primary inline" to="/sell">Create a sale <ArrowUpRight size={18}/></Link></div><div className="hero-figure"><span>Today's revenue</span><strong>{money(data.revenue)}</strong><small>{data.sales} completed {data.sales===1?'sale':'sales'}</small></div></section><div className="stats"><article><Package/><span>Collection</span><strong>{data.models}</strong><small>dress models</small></article><article><ShoppingBag/><span>In the atelier</span><strong>{data.pieces}</strong><small>pieces available</small></article><article className={data.low?'warn':''}><TriangleAlert/><span>Needs attention</span><strong>{data.low}</strong><small>low-stock colours</small></article></div><section className="section-head"><div><p className="eyebrow">QUICK START</p><h2>What would you like to do?</h2></div></section><div className="actions"><Link to="/inventory"><b>01</b><h3>Curate inventory</h3><p>Add a new model, colour or replenish stock.</p><ArrowUpRight/></Link><Link to="/sell"><b>02</b><h3>Ring up a dress</h3><p>Build a sale and print a polished receipt.</p><ArrowUpRight/></Link><Link to="/sales"><b>03</b><h3>Read the ledger</h3><p>Review daily totals and recent transactions.</p><ArrowUpRight/></Link></div></>}
