import {NavLink,Outlet,useNavigate} from 'react-router-dom';import {LayoutDashboard,Package,ReceiptText,ShoppingBag,LogOut,Wifi,WifiOff,RefreshCw} from 'lucide-react';import {motion} from 'framer-motion';import {logout,syncNow} from '../api';import {useSync} from '../hooks/useSync';
const nav=[[LayoutDashboard,'Overview','/'],[Package,'Inventory','/inventory'],[ShoppingBag,'New sale','/sell'],[ReceiptText,'Sales','/sales']] as const;
export function Shell(){const navigate=useNavigate(),sync=useSync();return <div className="app-shell"><aside className="sidebar"><img src="/brand/konooz-wordmark-transparent.png" className="side-logo"/><nav>{nav.map(([Icon,label,to])=><NavLink key={to} to={to} end={to==='/'}><Icon size={20}/><span>{label}</span></NavLink>)}</nav><button className="logout" onClick={async()=>{await logout();navigate('/login')}}><LogOut size={18}/> Sign out</button></aside><main><header className="topbar"><div><p className="eyebrow">ATELIER OPERATIONS</p><h1>Good day, Konooz</h1></div><button className={`sync-pill ${sync.online?'online':'offline'}`} onClick={()=>syncNow()}>{sync.syncing?<RefreshCw className="spin" size={15}/>:sync.online?<Wifi size={15}/>:<WifiOff size={15}/>}<span>{sync.syncing?'Syncing':sync.online?'Cloud ready':'Working offline'}</span></button></header><motion.div className="page" initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{duration:.28}}><Outlet/></motion.div></main><nav className="mobile-nav">{nav.map(([Icon,label,to])=><NavLink key={to} to={to} end={to==='/'}><Icon size={21}/><span>{label}</span></NavLink>)}</nav></div>}
